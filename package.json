{
  "name": "group-ride",
  "version": "1.7.2",
  "description": "Discord bot to organise group cycling rides",
  "workspaces": [
    "website"
  ],
  "type": "module",
  "module": "src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "test": "bun test",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "lint": "oxlint src tests",
    "lint:fix": "oxlint --fix src tests",
    "typecheck": "bunx tsc --noEmit",
    "website": "bun run --cwd website dev",
    "release": "git tag -a \"v$(node -p \"require('./package.json').version\")\" -m \"Release v$(node -p \"require('./package.json').version\")\" && git push --tags"
  },
  "dependencies": {
    "@grammyjs/conversations": "^2.1.1",
    "discord.js": "^14.26.4",
    "fast-xml-parser": "^5.9.3",
    "grammy": "^1.43.0",
    "pino": "^10.3.1",
    "staticmaps": "^1.13.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.51.0",
    "oxlint": "^1.66.0",
    "oxlint-tsgolint": "^0.23.0",
    "pino-pretty": "^13.1.3"
  }
}
